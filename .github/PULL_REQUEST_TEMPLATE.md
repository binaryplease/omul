<!--
Please read CONTRIBUTING.md first. One thing there is worth repeating here:
merging needs every author of every commit below to have signed the CLA
(CLA.md). You do not have to do anything before opening this — a check runs
here and comments with the sentence to reply with if a signature is missing.
Signing is once per account, and it is a license, not an assignment: your
copyright stays yours.
-->

## What this changes

<!-- One or two sentences. What is different afterwards, from the point of view
of whoever uses it. -->

## Why

<!-- The problem this solves. Link the issue if there is one. -->

## Requirement

<!-- Which REQxxx does this touch? If it touches one, its status and its
`## Updates` log should have moved in this same change. If it touches none, say
"none" and why. -->

- Requirement: `REQ___` / none
- Status moved to: `pending` / `in-progress` / `done` / `blocked` / n/a

## How you convinced yourself it works

<!-- What you ran, what you watched fail before the fix, what you deliberately
did not test. -->

- [ ] `mise run test` green
- [ ] `mise run build` green
- [ ] `mise run check` green, or it exits 127 on the `index` CLI this repo does
      not ship (say which)
- [ ] `docs/Requirements.md` left alone unless regenerated by `mise run
      requirements:index`
- [ ] [CLA.md](../CLA.md) signed by every author of every commit here. Leave
      this unticked when you open the pull request — the CLA check runs here and
      tells you what to reply with. Tick it once that check is green.
- [ ] Anything in this change that is not mine is marked with its source and
      its license (CLA §6.3), or there is nothing of the kind

## Anything a reviewer cannot see from the diff

<!-- Approaches you rejected, a constraint that forced an odd shape, a follow-up
you deliberately left. "Nothing" is a fine answer. -->
