---
id: REQ012
summary: Manage multiple-choice options
category: Question Types
priority: P1
source: BR012
status: done
tags:
  - multiple-choice
updated: "2026-07-28"
---

## Description

Options on a multiple-choice slide can be added, removed, reordered and renamed while the deck is editable, bounded by a per-slide option limit.

## Triage note

Add/remove/name verified by code inspection and by driving the running editor; per-option image/GIF enrichment deliberately not built (out of scope for task/0012).

## Updates

- 2026-07-28 — Verified rather than built: add / remove / name for answer options already existed in SlideEditor.tsx (add-option, per-option text input, remove button with a two-option floor enforced in the editor-document reducer), and was re-checked by driving the running editor. This slice extended the same option list with correct-answer marking (REQ013) and a selection limit (REQ014). Gap recorded deliberately: 'optionally enrich visually' is satisfied only by the auto-assigned per-option colour swatch — there is no per-option image/GIF upload, which was explicitly out of scope for task/0012 and remains unbuilt.
