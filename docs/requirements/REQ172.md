---
id: REQ172
summary: A stranger can run a production instance from this repository alone
category: Platform & Operations
priority: P0
source: internal
status: in-progress
tags:
  - publication
  - documentation
  - deployment
updated: "2026-09-07"
---

## Description

As a stranger who has cloned this repository and holds nothing else, I want a self-contained way to run a production instance — a compose file or equivalent in the tree, an environment-variable example, and a README path that ends at a working deployment — so that the repository is self-sufficient for the audience a published repository actually has, rather than only for a developer on the machine it was written on.

## Notes

Raised by a publication readiness check, **internal**. Nothing here is a disclosure; it is the buildability half of publication readiness, and it is the one that is easy to declare finished because the development path works.

**What existed when this was filed** (2026-08-27, corrected here on 2026-09-03 — the paragraph had gone stale in both directions and is dated rather than rewritten silently). `README.md` documented exactly one route: `nix develop`, which installs dependencies and starts the dev servers. That route is real and it works, and it stays. What followed it did not exist — no compose file, no `.env.example`, and no production section in the README. The `Dockerfile` built an image and `.github/workflows/build.yaml` published it to the container registry, so the artifact a self-hoster would run was produced; nothing in the repository told them how to run it, what to set, or what to persist.

By the time the work was picked up, part of that had already landed: `README.md` carried a `## Self-hosting` section with a working `docker run` invocation and a table of what breaks when each variable is unset, and `docs/deployment.md` carried the full environment reference. What was still missing was the self-contained half — a compose file and an environment example — which is what this requirement's change adds.

**Where the missing half currently lives, and why that is the problem.** The production compose file is delivered by the private infrastructure repository through NixOS `environment.etc` (REQ163, class 1). It is written for one host, it carries that host's topology, and it is not in this tree. So the only complete answer to "how do I run this" is held in a repository a stranger will never have — which is precisely the condition publication is supposed to end. The remedy is not to copy the private file across, which would carry the topology with it, but to write a self-hoster's compose file that owes nothing to that host: the published image, the state volume, the ports, and the environment variables the deployment must supply.

**What the environment example has to name, at minimum.** `BETTER_AUTH_SECRET` (see REQ171, which makes its absence fatal), `OMUL_BASE_HOST` as a bare host, `OMUL_TRUST_PROXY` where a reverse proxy terminates TLS, `DATABASE_PATH` and the fact that `auth.sqlite` and `admin.sqlite` resolve as siblings of it — so a volume mounted at the file rather than the directory loses two of three databases — and the administrator allowlist variable REQ164 introduces. The optional model-provider key is already documented and stays optional.

**Two README statements that did not survive the flip** (both settled since; corrected 2026-09-03). The `Public URL` line named the operator's own instance as though it were the project's — REQ163's class 1 work, and it swept the line out on 2026-08-28, so there is nothing left here to fix. The getting-started path being development-only was the other, and it is what the `## Self-hosting` section answered. A published README's first job is to get a stranger from clone to running instance; the deployment section is that path's second half, not an appendix.

**Not in scope.** The Nix development route, which works and stays. The private infrastructure repository's own compose file, which is that repository's business. Whether the project offers a hosted instance at all, which is a business decision and not a code requirement.

**Done when.** A self-hoster's compose file and an environment example ship in the tree; the README carries a production section that runs the published image from a clean machine with no access to any private repository; and a run performed from a fresh clone, following only the README, reaches a working instance.

## Updates

- 2026-08-27 — Filed by a publication readiness check. The development path is verified working; the production path exists only outside this repository. Recorded as a publication blocker under the rule that a stranger must be able to build *and run* the artifact from the public repository alone. No files added.
- 2026-08-28 — The missing production half is not missing everywhere: docker-compose.yml, docker-compose-dev.yaml and .env.example exist in the archived predecessor at commit c73eb72 on task/deployment-readiness-verified, together with a loadtest harness. They are a source to rewrite from and not a file set to copy — that compose file is written against MongoDB, and this product has run on SQLite since the storage layer was replaced, so the services, the volume and the environment table are all wrong in the same direction. The environment example there also predates BETTER_AUTH_SECRET failing closed (REQ171), the deployment-supplied administrator allowlist (REQ164) and OMUL_TRUST_PROXY. One thing in that commit deliberately does not come across: docs/security-papers/ is a set of operator and data-protection documents about a specific hosted service, including its subprocessors — a published code repository is not where they belong, and carrying them over would disclose the operator's processing arrangements to every clone.
- 2026-09-03 — compose.yaml and .env.example added at the repository root, README's ## Self-hosting section rewritten to lead with them, and docs/deployment.md pointed at both. compose.yaml: the published image, restart: unless-stopped, env_file .env, a named volume at /app/data, the container port published on 127.0.0.1 only, and a bun-based /api/health healthcheck (the image carries no curl or wget). A commented build: line builds the same image from the tree, so a clone needs no registry access at all. Nothing in it is written for a particular host — the reverse proxy, certificate and DNS name are the operator's and the file says nothing about them (REQ163 class 1). .env.example ships no working value: BETTER_AUTH_SECRET is an empty line with openssl rand -base64 32 in the comment (REQ171) and OMUL_ADMIN_EMAILS is empty (REQ164); OMUL_ALLOW_INSECURE_DEV_AUTH_SECRET is absent from it entirely. .dockerignore gained compose.yaml beside Caddyfile; .gitignore needed no change (it ignores the exact path .env, not .env*). Verified by building the image from this tree with the pinned base (which is anonymously pullable) and running the real compose.yaml against it: empty secret gives the fatal REQ171 crash naming the variable and the remedy; a filled one comes up healthy; /api/health returns ok, /api returns the discovery index, index.html serves 200; all three databases appear as siblings in the volume (omul.sqlite, auth.sqlite, admin.sqlite, plus -wal/-shm), which is the claim the mount-the-directory warning rests on; a deck created and joined by 6-digit code and an account signed up with no mail configured both survive up --force-recreate; docker port confirms 3000/tcp -> 127.0.0.1:3000 and the host's global address refuses the connection; setting OMUL_BASE_HOST + OMUL_TRUST_PROXY flips the boot posture lines to '1 proxy hop(s)' and 'advertises https://omul.example.com', /api then hands out https:// and wss:// links, and sign-in from that origin succeeds; OMUL_ADMIN_EMAILS given the same address in two cases yields '1 administrator(s)' lowercased and that account gets 200 from /api/admin/events while no session gets 401; a scheme-carrying OMUL_BASE_HOST is fatal at boot as documented. NOT VERIFIED, and the reason status stays in-progress rather than done: the literal fresh-clone run of REQ172's done-when pulls ghcr.io/binaryplease/omul:latest, and that is not anonymously pullable while this repository is private (docker manifest inspect answers 'unauthorized'). Every step of the README path other than that pull was exercised end to end against a locally built image of this same tree. Closing the requirement needs one human action after publication, or with registry credentials: clone, cp .env.example .env, put a generated secret in it, docker compose up -d, reach the instance. One discrepancy found and fixed in passing: server/email.ts reads BREVO_REPLY_TO_EMAIL and BREVO_REPLY_TO_NAME, which docs/deployment.md's environment list did not name; they are documented there now. No server behaviour or environment contract was changed. Gates: mise run check exit 0 (four steps, index check at 176 entries), mise run test 2342 pass / 0 fail across 126 files, mise run build exit 0, bun scripts/triage.ts validate 176 files valid. REQ172's Notes were corrected by hand where they had gone stale — the 'what exists today' paragraph (the README's Self-hosting section and the deployment reference had landed at the initial commit) and the public-URL statement (swept by REQ163 on 2026-08-28); both are dated in place rather than rewritten silently.
- 2026-09-07 — Publication happened (repository public 2026-09-07, release v1.0.0) and the done-when's blocking step is unchanged: the GHCR package was not flipped with the repository, so an anonymous pull is still refused — ghcr.io/token?scope=repository:binaryplease/omul:pull mints no token and the manifest answers 403, the tags endpoint 401 (measured 2026-09-07). The README's own first command therefore fails for the audience the requirement is written for. Documentation corrected rather than the requirement closed: README's Self-hosting quick start now leads with the build path (uncomment compose.yaml's build: line, docker compose up -d --build) and says why in one paragraph; the docker run block carries the same caveat; compose.yaml's header and image comment say it at the two places an operator reads; docs/deployment.md records that the build line is the working path and ties it to this requirement's status. No code, environment contract or compose service definition changed. Status stays in-progress: closing it needs the package made public and then the literal fresh-clone pull run once.
