---
id: REQ051
summary: Pin-on-image slide
category: Question Types
priority: P1
source: BR051
status: done
updated: "2026-08-06"
---

## Description

A slide type accepting pin coordinates on the image a slide references, rendering the aggregate placement. A slide carrying no image has no coordinate space and accepts no submissions at all.

## Updates

- 2026-08-06 — Pin on Image shipped end to end: 'pin-image' slide type in SlideTypeEnum/INTERACTIVE_SLIDE_TYPES, pins as per-mille "x,y" on the image's own lattice via encodePinPoint/decodePinPoint (one pin per participant, replaced on re-submission, off-image rejected not clamped), results payload carrying every pin plus the cloud's centre, and a shared PinCanvas (src/components/PinImage.tsx) worn by the participant's tap surface, the shared screen's heatmap, the editor preview and the target-area picker. Tests: server/pin.schemas.test.ts, server/pin.integration.test.ts, src/components/PinImage.test.ts; preview generator + PreviewPage coverage widened.
