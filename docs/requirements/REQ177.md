---
id: REQ177
summary: The storage layer is the published zodstore package, not a vendored snapshot
category: Platform & Operations
priority: P2
source: internal
status: done
tags:
  - dependencies
  - licensing
updated: "2026-08-31"
---

## Description

As a maintainer of this repository, I want the SQLite document store to be an ordinary registry dependency at a pinned version, so that updating it is a version bump with a changelog to read rather than a tree-diff against an upstream nobody can see from here.

## Notes

The store was vendored at `vendor/binp-docstore/` as `file:./vendor/binp-docstore` — a snapshot taken before the upstream had any version at all, sitting at `0.0.0`. Two costs followed from that and both are the reason this exists. **Updating it was a manual tree sync**, so "which storage layer is this?" could only be answered by diffing; and **its license had to be argued for** rather than stated, because the snapshot predated the upstream `LICENSE` file (REQ161, `NOTICE.md` §2). The upstream is now published as `@binaryplease/zodstore` under the MIT License, which settles both at once.

**It is a version jump, not only a specifier change.** The vendored snapshot predates the upstream's `0.1.0`; the published version is `0.4.2`. Three of the intervening changes reach this codebase and each is decided in `server/db.ts` with the reasoning beside it:

- **`enforceDefaults` (new in 0.1.0, defaults to `true`) is turned off here.** The library refuses a non-identity field with no `.default(...)` — this project's declared-defaults rule, enforced rather than documented — and recognises an identity field by the schema object its `ref()` helper returns. `ref(prefix)` is a `z.string().startsWith("prefix_")`, and this catalog's foreign keys are plain `z.string()` over `crypto.randomUUID()` values that carry no prefix, so the walk reads all eighteen of them — eight distinct names (`presentationId`, `slideId`, `workspaceId`, `userId`, `authorId`, `participantId`, `questionId`, `responseId`) spread over nine of the eleven collections — as ordinary fields that forgot a default. The rule is not dropped: `server/stored-defaults.test.ts` enforces it over every collection with each one's identity fields written down, so a *new* field that forgets a default still fails, and an exemption for a field that no longer exists fails too.
- **`maxRows` (new in 0.1.0, defaults to `10_000`) is disabled here.** An unbounded `find()` throws rather than truncating, which is the right shape — a silently truncated result is a wrong answer. It is still wrong for this application: every read is already narrowed by a presentation id, and one large event crosses ten thousand rows legitimately (1 000 participants over ten slides is 10 000 rows of `votes` for one deck, which `votes.find({ presentationId })` is what the results view and the spreadsheet export are built on). Capping it would turn that event into a 500. **This is the one place the swap leaves an open question rather than closing one** — the honest fix is a paged read at those two call sites, at which point the ceiling can come back on.
- **Two `undefined` readings changed (0.2.0) and both are strictly safer.** A `where` condition whose value is `undefined` now matches no row instead of being skipped, so `deleteMany({ ownerId })` with an unset id can no longer empty a collection; and an `update()` patch key holding `undefined` is now a no-op instead of resetting the field to its declared default. Nothing here relied on either, and the suite is unchanged by both.

The pragmas are left at the library's own defaults (`journal_mode=WAL`, `synchronous=NORMAL`, `busy_timeout=5000`); `applyDefaultPragmas`, which the vendored copy had and this codebase never passed, is gone.

**Done when.** No `vendor/` directory and no `file:` dependency in `package.json`; the store imported from `@binaryplease/zodstore`; `NOTICE.md` states the MIT position and carries the notice the bundled server has to travel with; and the repo's gate and test task stay green.

## Updates

- 2026-08-31 — Shipped. `bun add @binaryplease/zodstore@^0.4.2`, `git rm -r vendor/binp-docstore`, and the `file:` dependency removed from `package.json`; `server/db.ts` is the only importer and now passes `{ maxRows: null }` at the store and `{ enforceDefaults: false }` per collection, both commented with the reasoning above. New `server/stored-defaults.test.ts` (33 cases over the eleven collections) holds ADR-0029 with each collection's identity fields named, in three directions: every other field declares a default, every listed identity field is a real field, and every listed identity field carries no default. `scripts/licensing.test.ts` lost the four assertions over the vendored tree and gained five over the new position — the dependency is a registry specifier and not a `file:` path, no `vendor/` directory exists, `NOTICE.md` names the package and its license, it reproduces the MIT copyright and permission notice, and that reproduction is compared paragraph-by-paragraph against `node_modules/@binaryplease/zodstore/LICENSE` so a version that changes its notice cannot leave a stale copy standing. `NOTICE.md` §2 rewritten from "vendored first-party tree" to "MIT registry dependency" with the notice in full, and §5's dependency count re-measured today at 242 packages (195 MIT, 16 ISC, 11 Apache-2.0, 4 BSD-2-Clause, 3 MPL-2.0, 3 BSD-3-Clause, 2 MIT/X11, 2 OFL-1.1, 1 0BSD, 1 Unlicense, three compound grants, 1 stating none — still `buffers@0.1.1`, unchanged and still open). The Dockerfile no longer copies `vendor/` before `bun install`. Renamed through `AGENTS.md`, `README.md`, `.mise.toml`, `flake.nix`, `docs/architecture.md`, `docs/deployment.md`, `server/{schemas,accounts,admin-events,keyed-lock,store-path}.ts` and the 33 test headers that named the old package; `OPEN_SOURCING_PROGRESS.md` records the standing follow-up as closed. `docs/architecture.md` gains the two non-default options with their reasons. Left open and stated rather than papered over: with `maxRows: null` an unbounded `find()` is unbounded exactly as it was before the swap, so the >10 000-row read is a paging question this did not answer. mise run check exit 0 (the `index` CLI is present, so `docs/Requirements.md` is regenerated with this file's row); mise run build exit 0; mise run test 2300 pass / 0 fail across 126 files.
- 2026-08-31 — Review NST10460 finding 4 (minor, non-blocking): declining the library's per-collection ADR-0029 walk (enforceDefaults: false) put the rule in server/stored-defaults.test.ts over a hand-written list of eleven collections, and nothing asserted the list was complete — a twelfth createStore call under server/services/ would have been exempt in the library and in the test at once, silently, until a non-defaulted field made rows written before it stop parsing on read. server/db.ts now records what it opens (an openedCollections Set filled in createStore, read back through openedCollectionNames()), and the test loads the five service modules for their module-load side effects and fails on any opened collection that is not listed; the per-collection direction is checked too, so a stale row that nothing opens fails as well. Verified the guard is not vacuous: the registry reports all eleven names after importing services/presentations. docs/architecture.md's enforceDefaults bullet records the completeness check. The two declined defaults themselves stand as they were — maxRows: null is still this requirement's open question, and review finding 3 confirmed it is not a regression, since the vendored 0.0.0 snapshot had no ceiling either. mise run check exit 0; mise run test 2313 pass / 0 fail across 126 files; mise run build exit 0.
