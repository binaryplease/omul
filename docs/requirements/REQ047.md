---
id: REQ047
summary: 2×2 grid items
category: Question Types
priority: P1
source: BR047
status: done
updated: "2026-08-05"
---

## Description

The items a grid slide positions can be added, removed and renamed, bounded by a per-slide item limit.

## Updates

- 2026-08-05 — Slide.gridItems[] (GridItemSchema, default [], capped at GRID_ITEM_LIMIT = 8) authored in a new 'Items' editor section; each item is one vote row keyed by the existing VoteSchema.statementId — the multi-statement-scale shape, since grid items are judged independently — so results report placed/skipped counts and an average coordinate per item, with an explicit null for an item nobody placed (ADR-0024).
