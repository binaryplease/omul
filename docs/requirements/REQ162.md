---
id: REQ162
summary: A published repository has a reporting path and a contribution surface
category: Platform & Operations
priority: P0
source: internal
status: in-progress
tags:
  - publication
  - security
  - documentation
updated: "2026-09-03"
---

## Description

As the person who will hold this project publicly, I want `SECURITY.md` naming a live reporting path, `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md`, and issue and pull-request templates in place *before* the repository becomes public, so that the obligations publication creates — vulnerability reports, issues, a name in other people's dependency graphs — land somewhere that exists rather than in a tracker nobody is watching.

## Notes

Raised by a publication readiness check, **internal**.

**What exists today** (re-measured 2026-09-02; the filing state of 2026-08-25 was `README.md` alone at `health_percentage: 28`). All five files are in the tree and on the default branch: `SECURITY.md`, `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md`, `.github/ISSUE_TEMPLATE/` (three files) and `.github/PULL_REQUEST_TEMPLATE.md`. `gh api repos/<this remote>/community/profile` returns `health_percentage: 100` with the licence detected as `agpl-3.0`. Two and a half of the CLA's three parts landed the same day: `CLA.md` version 1.0, the terms stated in `CONTRIBUTING.md`, and the check in `.github/workflows/cla.yaml` — which **reports rather than enforces**, because `main` carries no branch protection and no ruleset naming it, so a red check does not stop a merge. Three things are still owed and none of them is in the tree: a required-status-check rule on `main`, the `cla-signatures` branch the check reads its signature store from, and a lawyer's read of the text. All three are in the closing entry in `## Updates`, with the `gh` commands for the first two in the workflow's own header. The issue tracker and the pull-request list are both empty, and the wiki — `has_wiki: true` on the repository — has never been initialised, so no `<repo>.wiki.git` exists to sweep. That is a small mercy and not a design: the wiki is enabled and will accept content the moment anyone writes to it.

**Why these are preconditions rather than follow-ups.** A public repository with no `SECURITY.md` receives its first vulnerability report as a public issue, which is a disclosure the project performs on itself. The private-vulnerability-reporting setting that gives a reporter a non-public channel only exists once the repository is public, so the file that points at it has to be in the tree at the moment of the flip, not added afterwards.

**The reporting path has to be real and monitored.** A `SECURITY.md` naming an address nobody reads is worse than none, because it converts a reporter who would have found another route into one who believes they have already reported. Name the address, the expected response window, and the supported versions.

**What the README still owes a stranger.** The current README is good on the happy path — `nix develop` and the dev servers come up — and it is honest about the one optional external service. As of 2026-09-02 it states the license (REQ161) and links the reporting path, so what is left of this paragraph is the part no file can settle: it has not been verified from a fresh clone in a clean environment with no credentials, which is the only test that counts.

**Scope note.** `README.md`, `AGENTS.md`, the design document, `docs/` and `task/` are all excluded from the Docker build by `.dockerignore` but are all part of the published tree — the publication surface is the repository, not the image.

**Contribution terms are decided: a CLA.** The owner has settled this one ahead of the license question, and it is recorded here because it binds independently of whichever license is eventually chosen. A **contributor license agreement (CLA)** is an agreement in which a contributor either assigns copyright in their contribution to the project owner or grants the owner a broad, sublicensable and relicensable right to it. The alternative that was on the table — a **developer certificate of origin (DCO)**, a per-commit sign-off asserting the contributor had the right to submit the code — establishes provenance but grants nothing, and so is not a substitute here.

What a CLA buys, and why the timing is not negotiable: the owner keeps the ability to relicense the project later, and to grant terms to a paying third party that the public license withholds. Both require holding the copyright in every line. A contribution that lands from an outside author without an agreement is owned by that author on whatever terms the public license grants, and there is no retroactive fix short of asking each contributor individually or removing the code. So the mechanism has to be in place before the repository can accept its first outside pull request — which, since a public repository can receive one within minutes, means before the flip.

This is not a claim that any particular license will be chosen. It is the precondition that keeps the choice open.

Three parts, all in the tree: the CLA text itself; an automated check that blocks an unsigned pull request (`cla-assistant` is the common one); and the requirement stated in `CONTRIBUTING.md` so a contributor meets it before writing code rather than after. Grafana is the closest reference — it runs a CLA and says so in its own `CONTRIBUTING.md`.

**Done when.** `SECURITY.md`, `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md`, `.github/ISSUE_TEMPLATE/` and `.github/pull_request_template.md` present; a CLA text in the tree with an enforcing check on pull requests and the requirement stated in `CONTRIBUTING.md`; the README states the license and links the reporting path; the community profile reports each file found; and the reporting address has an owner who has agreed to watch it.

## Updates

- 2026-08-25 — Filed by a publication readiness check. Community profile at 28%: README only, every other health file absent. Tracker, PR list and wiki all empty — nothing to redact there today, but the wiki is enabled. No files written; the contribution-terms question (developer certificate of origin vs. contributor license agreement vs. neither) is the owner's call and is not decided here.
- 2026-08-25 — Owner decided the contribution terms: a CLA, not a DCO and not nothing. Recorded on its own because it binds independently of the license, which remains open — a CLA is the precondition that keeps relicensing and third-party grants possible at all, and it expires as an option the moment the first outside pull request lands unsigned. Three parts owed: CLA text in the tree, an enforcing pull-request check, and the requirement stated in CONTRIBUTING.md. No license posture is implied or recorded by this entry.
- 2026-08-27 — 2026-08-27 re-check: unchanged. Community profile still 28% — README present, code_of_conduct, contributing, issue_template, pull_request_template and license all absent. Issue tracker and pull-request list still empty (gh issue list -s all, gh pr list -s all return nothing) and the wiki still has no repository at all (the wiki remote reports Repository not found), so there is still nothing to redact on those surfaces. The CLA decision from 2026-08-25 stands and none of its three parts is in the tree yet.
- 2026-08-28 — Four of the five files this requirement owes already exist, written and reviewed, in the archived predecessor at commit c73eb72 on task/deployment-readiness-verified: SECURITY.md, CONTRIBUTING.md, CODE_OF_CONDUCT.md and TRADEMARK.md, alongside the LICENSE REQ161 names. None of them survived the clean-slate snapshot and none is in this tree. They are recoverable and should be brought to the current state rather than copied — that CONTRIBUTING.md predates the CLA decision of 2026-08-25 and the TRADEMARK.md is written for the previous product name (REQ170). Issue and pull-request templates are not in that set and are still owed outright. The licensing mailbox REQ161 now names is a second monitored address beside the security one, and the same rule applies to it: an address nobody reads is worse than none. The CLA's three parts — text, enforcing pull-request check, and the requirement stated in CONTRIBUTING.md — remain unbuilt, and the licensing decision has raised their cost of being late: the commercial license offered beside AGPL-3.0-only is only offerable while copyright in every line is held, so a single outside pull request merged without a signature removes the option for that code permanently.
- 2026-09-02 — 2026-09-02 re-check, and this is the entry where most of this requirement closes. All five files are in the tree and on origin/main, and all five arrived at this repository's initial commit 15ee1bb (feat: omul 1.0.0, 2026-09-01) rather than in work of their own — so the 2026-08-28 entry's 'none of them survived the clean-slate snapshot' describes the earlier snapshot, not this tree. Present: SECURITY.md (72 lines), CONTRIBUTING.md (141), CODE_OF_CONDUCT.md (128), .github/ISSUE_TEMPLATE/ with bug_report.yml, feature_request.yml and config.yml, and .github/PULL_REQUEST_TEMPLATE.md. gh api repos/binaryplease/omul/community/profile now returns health_percentage 100 against 28 at filing, with code_of_conduct detected as contributor_covenant, contributing, pull_request_template and license (agpl-3.0, from LICENSE-AGPL-3.0) all resolving to files. One field reads oddly and is not a gap: files.issue_template is null while the score is 100 — that field reports the legacy single-file .github/ISSUE_TEMPLATE.md path, and the directory form this tree uses is what the score counts. The reporting path is real: SECURITY.md gives two private routes, GitHub private vulnerability reporting and support@hyhyve.com with 'omul security' in the subject, the same mailbox REQ161's 2026-08-30 entry put on record as monitored and owner-chosen, and it publishes response targets as a table (acknowledge 5 working days, trace 10, fix by severity, advisory with the release) while saying plainly that these are targets and neither license carries a support obligation. The issue-template config.yml routes a would-be security issue to the advisory form before the reporter reaches a text box, and bug_report.yml repeats it. README links both the license and the reporting path. What is still owed is the CLA and nothing else: no CLA text in the tree, no enforcing pull-request check, and .github/workflows/ holds only build.yaml. CONTRIBUTING.md answers the third part in the only way it can before the text exists — it names the CLA as the decided instrument, says the text is being drafted with legal advice, and states that pull requests are read and reviewed but not merged until it lands, with issues and discussion explicitly open. That policy hold is what currently protects the dual-license position; it is a promise rather than a check, so an unsigned outside contribution is still one merge away from being unfixable, and the repository is private today (visibility private), which is the only reason the exposure is zero. One contradiction found and fixed here: .github/PULL_REQUEST_TEMPLATE.md still told contributors the terms '(DCO / CLA / neither) are not settled yet', which the owner's decision of 2026-08-25 made false and which contradicted CONTRIBUTING.md three files away; it now states the CLA as decided and the hold as pending its text. Two smaller gaps left standing rather than papered over. SECURITY.md names no supported versions, which this requirement's own Notes ask for — arguably moot while there is one tag and no release series, but it is not written down. And the guard in scripts/licensing.test.ts that keeps the contact address singular and unstale covers LICENSE-COMMERCIAL and TRADEMARK.md only, while the same address now also appears in SECURITY.md, README.md, CODE_OF_CONDUCT.md and .github/ISSUE_TEMPLATE/config.yml, none of which any test holds. Tracker and pull-request list still empty, wiki still has no repository (the wiki remote reports Repository not found), so there is still nothing to redact on those surfaces. Status moves pending to in-progress: the five-file half is done, the CLA half is not, and neither the CLA text nor the sign-off on the reporting mailbox is work this session can produce. Notes corrected in the same change where they had gone false: the 'What exists today' paragraph and the README clause both described the 2026-08-25 state.
- 2026-09-02 — CLA landed: text, enforcing check, and the requirement stated where a contributor meets it. CLA.md is version 1.0 — a license grant, not an assignment, which is what CONTRIBUTING.md had already promised outside contributors and so was the only shape available. Structure follows the Apache ICLA that Grafana's is built on (this requirement named Grafana as the closest reference): §2 copyright grant, §3 patent grant with Apache-style defensive termination, §6 representations, §8 no warranty, §9 no obligation to use. Three additions the Apache form does not carry, each forced by omul's position. §2 spells out the relicensing right in as many words — 'under any license terms the Owner chooses' — because Apache's form relies on the Foundation's outbound freedom rather than stating it, and a dual license needs it stated. §5 is the consideration back, in the Harmony OSI-approved shape: every accepted contribution stays available to the public under AGPL-3.0-only or another OSI- and FSF-approved free license, the commercial half is offered beside it and never instead of it, and if that undertaking is broken the relicensing right in §2 reverts to AGPL-3.0-only alone while sublicenses already granted survive. §4 addresses German moral rights (Urheberpersoenlichkeitsrechte), which cannot be assigned under the law §11 chooses, so the file licenses and asks for non-assertion rather than pretending to transfer them. Enforcement is .github/workflows/cla.yaml, contributor-assistant/github-action@v2.6.1 on pull_request_target plus issue_comment, signatures committed to signatures/v1/cla.json on a cla-signatures branch of this repository rather than a remote one, so GITHUB_TOKEN suffices and no PAT secret is owed before the check works. Versioned path because §10 makes a new version non-retroactive. Allowlist covers the copyright holder, who cannot sign an agreement with themselves, and bots, which have no copyright to grant. Concurrency group is per pull request with cancel-in-progress false: signatures append to one JSON file and a cancelled run can be mid-commit. The policy hold this requirement recorded on 2026-09-02 is lifted in the same change, in all three places that stated it — CONTRIBUTING.md now gives the terms and the signing route instead of the hold, .github/PULL_REQUEST_TEMPLATE.md tells a contributor the check will ask rather than that their work will sit, and README.md's contributing bullet no longer says the terms are unsettled. Two guards added to scripts/licensing.test.ts, which is the only thing in this repository that executes a license claim. CLA.md joins LICENSE-COMMERCIAL and TRADEMARK.md in the contact-route check, so it names support@hyhyve.com and no second address. A new block holds the drift this file was written before rather than after: the signing sentence lives in CLA.md once and in the workflow twice, once in the if: guard and once as custom-pr-sign-comment, all three compared literally, so a contributor who copies it out of the document and gets no acknowledgement would have no route at all — the check that blocks the merge is the same one that records the signature. It also holds that the grant never becomes an assignment, that §5 keeps its two approving bodies, that the three contributor-facing files point at CLA.md and describe it the same way, and that none of them still says the terms are unsettled. 2333/2333 tests green, mise run check green including index check. Status stays in-progress, not done: what this requirement's Done when clause lists is now all true, but its own Notes ask SECURITY.md for supported versions and it still names none, and the contact guard now covers three files while the same address also appears unguarded in SECURITY.md, README.md, CODE_OF_CONDUCT.md and .github/ISSUE_TEMPLATE/config.yml. Both were flagged in the previous entry and neither is closed here. One thing this session cannot settle and did not invent around: §11 chooses German law and leaves venue as the Owner's place of business rather than naming a city, and the whole document is drafted from the standard forms rather than by a lawyer — the previous entry recorded the owner's intent to draft with legal advice, so a review of §5, §7 and §11 before the repository goes public is the outstanding item on the text itself.
- 2026-09-02 — Fix pass after the security review of the CLA slice (NST10690, verdict land-after-fixes). Two corrections first, because the previous entry put false statements on an append-only record and they can only be corrected by appending. One: that entry said 'what this requirement's Done when clause lists is now all true'. It is not. Done when asks for an enforcing check on pull requests, and main carries no branch protection and no ruleset, so a red CLA check does not stop a merge — it reports, it does not enforce. The status was left in-progress for the wrong reason and stays in-progress for this one. Two: that entry said the allowlist 'covers the copyright holder'. It never did. binaryplease is an Organization account, commits are authored by user accounts, so the entry matched nothing — and worse, the review showed it was not merely inert. Finding 1, the only High: contributor-assistant compiles an allowlist entry containing an asterisk into an unanchored regex and matches it against a name that comes from the raw git author object whenever the commit author's email is linked to no GitHub account. That name is whatever git config user.name was set to and GitHub never authenticates it, so bot* matched any name containing 'bot' and a stranger could make this check report green with nothing signed; an entry without an asterisk is an exact compare a spoofed display name satisfies just as easily. The fix is no allowlist at all rather than the narrower one the review proposed, because every entry is self-declarable by that path. With none, an unlinked author carries an empty id, matches no signed record and stays in notSigned, so the check fails closed. The accepted cost is that Dependabot's own pull requests will show the check red and a maintainer merges them on their own authority; that is written into both the workflow header and .github/dependabot.yml. Six more workflow fixes in the same file. The action is pinned to commit ca4a40a7d1004f18d9960b404b97e5f30a505a08 rather than the mutable tag v2.6.1, since this job holds a token that can push anywhere in this repository, and a new .github/dependabot.yml maintains that pin on the github-actions ecosystem only — npm is deliberately excluded so a stream of bumps does not outrun NOTICE.md section 5's measured enumeration. The comment gate is contains() rather than exact equality: the action's own matcher compares body.trim().toLowerCase(), so an equality gate rejected a reply with a trailing newline or different capitalisation before the job started, leaving the contributor no comment, no check update and no error — verbatim the dead end the sentence-lock test was written to prevent, and the test could not see it because it locked the string rather than the comparison. A step refuses a pull request with more than 100 commits, because the action reads one commits(first: 100) GraphQL page and never follows pageInfo, so authors of commits 101 and beyond went unchecked; the step interpolates nothing attacker-controlled, only an integer in its if: and two literal lines of script. The concurrency group is now constant rather than per pull request, because the shared resource is one JSON file and two pull requests keyed separately would read-modify-write it concurrently, giving the loser a 409 and a contributor who signed correctly a red check. statuses: write is dropped, since grepping the pinned version finds no createCommitStatus and no checks call — the action signals purely through job exit status — and actions goes to read rather than write: its rerun convenience lists this repository's workflows and runs unguarded on the signing path, so removing the scope entirely would fail that path, while the rerun call itself needs a PAT this workflow does not supply and is already wrapped in its own catch. path-to-document is a commit permalink instead of blob/main/, because the signature record stores no document version, so that URL is the only evidence of what a signer agreed to and section 10 makes a new version non-retroactive. Nine tests added to scripts/licensing.test.ts, one per failure scenario: no allowlist key, a 40-character SHA pin with the version in a trailing comment, a Dependabot github-actions entry, contains() and never equality on the comment body, the commit bound, a constant concurrency group with cancel-in-progress false, no statuses scope and actions at read, no actions/checkout and no untrusted interpolation, and a document URL pinned by owner, repository and commit rather than merely ending in CLA.md. Three existing assertions the review called overstated were tightened in place: the anti-assignment negative is now case-insensitive and covers the phrasings an assignment clause is written in rather than one literal, and a new test holds section 5's reversion remedy and its survival clause for already-granted sublicenses, which no assertion covered and which is the half that makes the free-software undertaking cost anything. Two Info frictions fixed: the pull-request template asked a contributor to tick a CLA box in the same file that told them nothing was required before opening, and now says to leave it unticked until the check is green; CONTRIBUTING.md's 'nothing to print and nothing to email' now names the two cases that do use email, the employer waiver and a signer without a usable GitHub account. Two findings are repository state and cannot be fixed from the tree, so they are written down where a maintainer will find them — both as runnable gh commands in the workflow header, and both still owed. Finding 2: the cla-signatures branch does not exist and signatures/v1/cla.json with {"signedContributors": []} has to be created before the first outside pull request, because the action's own create-on-404 bootstrap is dead in v2.6.1, comparing a numeric error.status against the string "404", so every run today fails with an opaque 'Could not retrieve repository contents. Status: 404'. Finding 3: a ruleset on main naming the status check 'Check the contributor license agreement' is what turns this from advisory into enforcing, and the Done when clause is not met until it exists. Findings 1 through 6 are inert while the repository is private, forks 0 and no pull requests open, and all of them arm at the publication flip this requirement gates. Gates: mise run test 2342 pass 0 fail across 126 files, up from 2333 by the nine new tests; mise run check exit 0, all four steps green including index check at 176 entries. Not done and not attempted: the legal read of sections 5, 7 and 11 still outstanding from the previous entry, SECURITY.md's supported versions, and extending the contact guard to SECURITY.md, README.md, CODE_OF_CONDUCT.md and .github/ISSUE_TEMPLATE/config.yml.
- 2026-09-03 — Both in-repo gaps the last two entries flagged are closed here; the three out-of-repo items are untouched and restated below, because none of them is work a session can do. Gap one — SECURITY.md named no supported versions, which these Notes ask for in as many words. It now carries a Supported versions section between the response-target table and Scope, and the claim is deliberately small because the tree cannot stand behind a bigger one: there is one release series, 1.0.0 in package.json, this repository holds no tags at all, and nothing older was ever published, so the table says 1.x and main get fixes and that nothing earlier exists. It states what follows rather than leaving it implied — no long-term-support branches and no back-ports to earlier tags, a fix lands on main and goes out with the next release while ghcr.io/binaryplease/omul:latest is rebuilt from main by build.yaml, and a reporter who can only reach an older build should name the commit or image tag and report anyway — plus a promise that a change to any of this appears in that section before it takes effect, since a supported-version claim going stale silently is the failure mode a stranger pays for. Gap two — the contact guard in scripts/licensing.test.ts covered three files while the address appeared in seven. The set is now all seven: LICENSE-COMMERCIAL, TRADEMARK.md, CLA.md, SECURITY.md, README.md, CODE_OF_CONDUCT.md and .github/ISSUE_TEMPLATE/config.yml, each asserted to name support@hyhyve.com, to name no second address, and to carry no UNRESOLVED banner. Two shapes changed rather than being extended unaltered. URLs are stripped before the address scan, because config.yml is a list of links rather than prose and a URL can carry an at-sign (git@host, a scoped package path) while naming no mailbox at all, so the assertion now means every mail address in the file is the contact rather than every at-shaped token. And the RFC 2606 documentation domains are exempt, which is what lets README.md into the set at all: its docker run example says you@example.com, a domain reserved precisely so it can appear in documentation, and one that cannot rot into a dead route because it was never a live one. The block comment was rewritten rather than left stale — it still says why those three files are the only route a reader has, and now says what the other four are, that SECURITY.md is the one a stranger reaching for a vulnerability report reads, and why the two exemptions exist. One test was added beyond the extension, because the failure being closed is the list going stale silently rather than any single file drifting: every file at the repository root and under .github, tracked or not yet ignored — the same candidate set guard-publication-residue.ts uses, for the same reason, that the post-session hook is what commits — is scanned, and the set of them naming a mailbox has to be exactly the seven held above. A new root file that publishes an address fails; a held file that quietly drops the address falls out of the set and fails. Both guards were mutation-checked rather than assumed: a second address appended to SECURITY.md fails the per-file assertion, an untracked scratch file at the root naming an address fails the surface assertion, and the tree was restored after. Gates: mise run test 2351 pass 0 fail across 126 files, up from 2342 by the nine new tests (seven files times two, plus the surface test, against three times two before); mise run check exit 0 with all four steps green, including index check at 176 entries, so the index CLI was on PATH and ran rather than exiting 127. No Notes paragraph went false and none was corrected: the one asking for the address, the response window and the supported versions is an instruction rather than a claim about the tree, and it is now satisfied. Status stays in-progress, for the three out-of-repo items, restated here so they are not lost: the cla-signatures branch holding signatures/v1/cla.json with an empty signedContributors array does not exist, so the CLA check fails with an opaque 404 until a human creates it, and the gh commands are in the header of .github/workflows/cla.yaml; main carries no branch protection and no ruleset naming the status check Check the contributor license agreement, so that check reports rather than enforces and the Done when clause is not met; and a lawyer's read of CLA.md sections 5, 7 and 11 is still outstanding. None was attempted here, and nothing written here assumes any of them has happened.
