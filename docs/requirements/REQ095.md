---
id: REQ095
summary: Spreadsheet export
category: Results & Export
priority: P1
source: BR095
status: done
updated: "2026-08-07"
---

## Description

A deck's results can be downloaded as a spreadsheet: raw answers keyed by participant and slide, plus the aggregates the results surface shows.

## Updates

- 2026-08-06 — XLSX export shipped: GET /api/presentations/:id/results.xlsx (owner-or-edit-token, attachment, no-store) built by the pure server/results-export.ts — Summary, Slides, Responses, Participants matrix and tidy-long Aggregates sheets; answers decoded through the vote endpoint's own codecs and tallies read from getAllResults rather than recomputed. Export chip on the presenter surface downloads it under the server's filename via the shared src/download.ts helper.
- 2026-08-07 — Review cleanup: the title-to-filename slug existed twice, character for character, in server/results-export.ts and src/download.ts. It is now one descriptor, deckFilenameSlug in server/schemas.ts (re-exported through src/types.ts), composed by resultsExportFilename for the workbook and by HomePage for the JSON deck export (ADR-0026). Also collapsed the two identical describeAnswer branches (word-cloud/open-text and scale) into one case group. No change to any exported cell or filename; docs/frontend.md and docs/architecture.md updated.
