---
id: REQ046
summary: 2×2 grid slide
category: Question Types
priority: P1
source: BR046
status: done
updated: "2026-08-05"
---

## Description

A slide type where a participant places each item at a coordinate in a two-dimensional field; the tally reports the mean coordinate per item.

## Updates

- 2026-08-05 — 2x2 Grid slide type shipped end to end: 'grid' joins SlideTypeEnum/INTERACTIVE_SLIDE_TYPES; participants place each item with one slider per axis (drag rejected for the same phone-scroll and keyboard/screen-reader reasons ranking used, doubled for a 2-D drag) on a shared GridPlot field; getSlideResults averages the coordinate per item and returns the individual placements behind it; presenter and participant render the quadrant plot with a per-item legend. Repo left on main, so task: stays unset.
